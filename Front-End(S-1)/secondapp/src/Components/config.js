const app_config={
    api_url:'http://localhost:5000'
}
export default app_config

// We have made config file in both frontend and backend because if we have to host the website and have to
//  chnage the port so we chnage the port in config file and can access in all the website..we don't need to 
// change everywhere.
